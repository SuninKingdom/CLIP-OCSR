# Note: Install PyTorch separately first (see README.md)
# torch and torchvision are excluded here to avoid version conflicts

# Core dependencies
numpy>=1.21.0
pandas>=1.3.0
Pillow>=9.0.0
tqdm>=4.60.0
scikit-learn>=1.0.0
pyyaml>=5.4.0

# Tokenizer
tokenizers>=0.12.0

# Chemistry
rdkit>=2022.03.0

# Logging
tensorboard>=2.10.0

# CLIP (install separately)
# pip install git+https://github.com/openai/CLIP.git
